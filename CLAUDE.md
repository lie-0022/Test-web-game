# CLAUDE.md

이 레포에서 작업하는 Claude를 위한 가이드. 행동 원칙 + 프로젝트 컨텍스트.

행동 원칙은 [andrej-karpathy-skills](https://github.com/forrestchang/andrej-karpathy-skills)를
참고했다. LLM의 흔한 코딩 실수(추측, 과설계, 무관한 변경, 검증 부재)를 줄이는 데 목적이 있다.

**트레이드오프:** 이 원칙들은 속도보다 신중함에 치우쳐 있다. 사소한 작업은 판단껏.

---

## 행동 원칙

### 1. 코딩 전에 생각하기

**추측하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.**

구현 전에:
- 가정을 명시할 것. 불확실하면 물어볼 것.
- 해석이 여러 가지면 모두 제시할 것 — 조용히 하나 고르지 말 것.
- 더 단순한 방법이 있으면 말할 것. 필요하면 반대 의견을 낼 것.
- 불명확하면 멈출 것. 무엇이 헷갈리는지 짚고, 물어볼 것.

### 2. 단순함 우선

**문제를 푸는 최소 코드. 투기적인 것은 없음.**

- 요청되지 않은 기능 금지.
- 한 번 쓰는 코드에 추상화 금지.
- 요청되지 않은 "유연성"·"설정 가능성" 금지.
- 불가능한 시나리오에 대한 에러 핸들링 금지.
- 200줄을 썼는데 50줄로 될 것 같으면 다시 쓸 것.

자문할 것: "시니어 엔지니어가 이걸 과하다고 할까?" 그렇다면 단순화.

### 3. 외과적 변경

**꼭 필요한 것만 건드릴 것. 내가 만든 것만 치울 것.**

기존 코드를 수정할 때:
- 인접 코드·주석·포맷을 "개선"하지 말 것.
- 망가지지 않은 것을 리팩터링하지 말 것.
- 내 취향과 달라도 기존 스타일에 맞출 것.
- 무관한 죽은 코드를 발견하면 언급만 할 것 — 지우지 말 것.

내 변경이 고아 코드를 만들면:
- 내 변경 때문에 미사용이 된 import/변수/함수는 제거할 것.
- 기존부터 죽어 있던 코드는 요청 없이는 제거하지 말 것.

기준: 바뀐 모든 줄은 사용자 요청으로 직접 추적되어야 한다.

### 4. 목표 주도 실행

**성공 기준을 정의할 것. 검증될 때까지 반복할 것.**

작업을 검증 가능한 목표로 바꿀 것:
- "검증 추가" → "잘못된 입력에 대한 테스트를 쓰고, 통과시킨다"
- "버그 수정" → "버그를 재현하는 테스트를 쓰고, 통과시킨다"
- "X 리팩터링" → "리팩터링 전후로 테스트가 통과하는지 확인한다"

다단계 작업은 짧은 계획을 명시할 것:
```
1. [단계] → 검증: [확인]
2. [단계] → 검증: [확인]
```

---

## 프로젝트 컨텍스트

**Test-web-game** — 모바일 브라우저에서 실행되는 2D 모바일 웹게임.

현재 단계: 기술 스택 리서치 완료, 코드 스캐폴딩 전.

### 확정된 스택

- **차원:** 2D
- **프레임워크:** Phaser 3
- **언어:** TypeScript
- **빌드 도구:** Vite (공식 `phaserjs/template-vite-ts` 기반 예정)
- **오디오:** Howler.js

상세 근거와 리서치는 [`docs/mobile-web-game-research.md`](docs/mobile-web-game-research.md) 참조.

### 모바일 웹게임 작업 시 지킬 것

- **모바일 우선** — viewport meta, Scale Manager `FIT`, `touch-action: none`, 44px+ 터치 타깃,
  `devicePixelRatio` 캡. 데스크톱 `click`이 아니라 `touchstart`/`touchend` 사용.
- **성능** — 고정 저해상도 렌더 + 업스케일, 오브젝트 풀링(프레임당 할당 0), 고정 타임스텝 루프,
  Arcade 물리 + 단순 콜라이더, 프로덕션 빌드는 물리 `debug: false`.
- **에셋** — 텍스처 아틀라스, 작은 스프라이트 다수(`tileSprite` 남용 금지), 비트맵 폰트,
  씬별 프리로드, 세션 페이로드 ~3–5MB 유지.
- **검증** — 변경 후 실제 저사양 안드로이드 / iOS Safari 동작을 확인. 가능하면 dev 서버를 띄워
  브라우저에서 직접 테스트하고, 불가능하면 그렇다고 명시할 것.

### 작업 규칙

- 개발 브랜치: `claude/mobile-game-research-Q2MIb`. 명시적 허락 없이 다른 브랜치에 푸시 금지.
- 문서는 한국어로 작성(README, docs와 일관성 유지).
- 사용자가 요청하지 않으면 PR을 만들지 말 것.

---

**이 가이드가 작동하고 있다는 신호:** diff에 불필요한 변경이 줄고, 과설계로 인한 재작성이 줄고,
실수 후가 아니라 구현 전에 질문이 나온다.
